const User = require("../database/prisma").user;

const createRating = async (req, res) => {
  try {
    
  } catch (error) {
    console.error(error);
    
  }
};

module.exports = { createRating };
